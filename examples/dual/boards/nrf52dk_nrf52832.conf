# nRF52 is hard-float: the prebuilt lib is thumbv7em-none-eabihf.
CONFIG_FPU=y
CONFIG_FP_HARDABI=y
# 64 KB RAM cannot hold a dual session; not recommended on nRF52832.
CONFIG_HEAP_MEM_POOL_SIZE=49152
